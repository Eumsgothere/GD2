public int life = 5;

public void TakeDamage(int amount)
{
    if (!canMove) return;

    life -= amount;

    if (life <= 0)
    {
        life = 0;
        GameUI.Instance.ShowLose(); // if you have UI
        canMove = false;            // disable controls
        // trigger die animation if you have one
    }
}