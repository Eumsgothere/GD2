using UnityEngine;

public class RedSphere : MonoBehaviour
{
    private void OnTriggerEnter(Collider other)
    {
        if (other.CompareTag("Player")) // just use the player tag for simplicity
        {
            Destroy(gameObject);
        }
    }
}