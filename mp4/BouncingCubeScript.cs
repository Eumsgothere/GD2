using UnityEngine;

public class BouncingCubeScript : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
    //private void OnCollisionEnter(Collision collision)
    //{
        //MeshRenderer s = collision.collider.GetComponent<MeshRenderer>();
        //s.material.color = new Color(Random.Range(0f, 1f), Random.Range(0f, 1f), Random.Range(0f, 1f)); ;
        //GetComponent<MeshRenderer>().material.color = new Color(Random.Range(0f, 1f), Random.Range(0f, 1f), Random.Range(0f, 1f));

        //Debug.Log("Enter");
    //}
    //private void OnCollisionExit(Collision collision)
    //{
    //    Debug.Log("Exit");
    //}
    //private void OnCollisionStay(Collision collision)
    //{
    //    Debug.Log("Stay");
    //}
}
